<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.manage.cattle.dao.UserDao">
    <select id="login" resultType="com.manage.cattle.dto.UserDTO">
        select
            id,createUser,createTime,updateUser,updateTime,
            username,name,roleId
        from t_user
        where username=#{username} and password=#{password}
    </select>

    <select id="getUser" resultType="com.manage.cattle.dto.UserDTO">
        select
            id,createUser,createTime,updateUser,updateTime,
            username,name,roleId
        from t_user
        where username=#{username}
    </select>

    <select id="listUser" resultType="com.manage.cattle.dto.UserDTO">
        select
               id,createUser,createTime,updateUser,updateTime,
               username,name,roleId
        from t_user
        where 1=1
        <if test="username!=null and username!=''">
            and username like concat('%',#{username},'%')
        </if>
        <if test="name!=null and name!=''">
            and name like concat('%',#{name},'%')
        </if>
        order by username
    </select>
</mapper>